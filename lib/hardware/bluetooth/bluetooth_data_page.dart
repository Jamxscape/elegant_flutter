import 'package:flutter/material.dart';

class BlueToothDataPage extends StatelessWidget {
  const BlueToothDataPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
